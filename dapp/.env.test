# If you add variables here, make sure to add them to the check in
# src/index.tsx too.

STAKING_CONTRACT=0xstaking
REWARDS_MASTER_CONTRACT=0xrewardsMaster

# SUGGESTED_RPC_URL doesn't have to be set, but can override default
# suggestion when the network is missing and wallet_addEthereumChain
# is called to add it.

# For use with Hardhat Network
CHAIN_ID=31337
SUGGESTED_RPC_URL=http://127.0.0.1:8545/

SASS_PATH=node_modules:src
